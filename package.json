{
  "name": "rusticon",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist pkg docs target",
    "build:wasm:dev": "wasm-pack build --target web --dev",
    "build:wasm": "wasm-pack build --target web --release",
    "build:site": "vite build",
    "build:web": "pnpm run build:wasm && pnpm run build:site",
    "build:terminal": "cargo build --release --bin rusticon && mkdir -p dist && cp target/release/rusticon dist/",
    "build:macos": "cargo build --release --bin rusticon_macos --features macos-native && mkdir -p dist && cp target/release/rusticon_macos dist/",
    "bundle:macos": "node scripts/bundle.js",
    "build:all": "rm -rf dist && pnpm run build:web && pnpm run build:terminal && pnpm run build:macos && pnpm run bundle:macos",
    "build": "cargo update && pnpm run build:all",
    "dev": "cargo update && pnpm run build:wasm:dev && vite",
    "dev:watch": "cargo watch -s 'pnpm run build:wasm' & vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "toml": "^4.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vite-plugin-wasm": "^3.6.0"
  },
  "packageManager": "pnpm@11.3.0+sha512.2c403d6594527287672b1f7056343a1f7c3634036a67ffabfcc2b3d7595d843768f8787148d1b57cf7956c90606bbd192857c363af19e96d2d0ec9ec5741d215"
}
